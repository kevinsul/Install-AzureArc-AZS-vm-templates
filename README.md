# Install-AzureArc-AZS-vm-templates

These two ARM templates are simply samples of how one can go about installing the Azure Arc agent when deploying a Windows Server VM or Linux VM (Ubuntu) using an ARM template.
